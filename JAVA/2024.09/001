/*
1.정수 num과 n이 매개 변수로 주어질 때, num이 n의 배수이면 1을 return n의 배수가 아니라면 0을 
return하도록 solution 함수를 완성해주세요.
입출력 예시 : 
num  	n  	result
98	  2	    1
34	  3	    0

*/


class Solution {
    public int solution(int num, int n) {
        int answer = 0;
        
        if(num%n==0) {
	        		System.out.println(answer=1);
	        	}else {
	        		System.out.println(answer=0);
	        	}
	        return answer;
    }
}

/*
다른풀이1)삼항연산자 활용
class Solution {
    public int solution(int num, int n) {
        int answer = num % n == 0 ? 1 : 0;
        return answer;
    }
}

다른풀이2)리턴값 활용
class Solution {
    public int solution(int num, int n) {
        int answer = num % n == 0 ? 1 : 0;
        return answer;
    }
}*/
